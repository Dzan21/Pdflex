{
  "name": "pdflex",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"cd pdflex-auth && pnpm dev\" \"cd pdflex-web && pnpm dev\"",
    "build": "pnpm --filter pdflex-web build",
    "auth": "pnpm --filter pdflex-auth dev",
    "web": "pnpm --filter pdflex-web dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
