{
  "name": "pdflex",
  "private": true,
  "scripts": {
    "dev:auth": "pnpm --filter pdflex-auth dev",
    "dev:web": "pnpm --filter pdflex-web dev",
    "build": "pnpm --filter pdflex-web build"
  }
}
